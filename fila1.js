let todoItems=[];
function firstname(text){
    const firstname={
        text,
        checked:false,
        id:DataTransfer.now(),
    };
    firstnameItems.push(firstname);
    console.log(firstname);
}